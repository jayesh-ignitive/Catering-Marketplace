# NestJS catalog API (sibling project: catering-backend). Default: http://localhost:4000
NEXT_PUBLIC_CATERING_API_URL=http://localhost:4000

# Optional: shown after signup as workspace host (must match backend SUBDOMAIN_PARENT_DOMAIN)
# NEXT_PUBLIC_SUBDOMAIN_PARENT_DOMAIN=catering.test
