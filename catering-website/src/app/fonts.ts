import { Great_Vibes } from "next/font/google";

/** Script wordmark — loaded only on layouts that render `font-logo`. */
export const greatVibes = Great_Vibes({
  variable: "--font-great-vibes",
  subsets: ["latin"],
  weight: "400",
  display: "swap",
});
