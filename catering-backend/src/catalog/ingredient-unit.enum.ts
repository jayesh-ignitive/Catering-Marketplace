/** Stored as MySQL ENUM on `ingredients.purchase_unit` / `consumption_unit`. */
export enum IngredientUnit {
  KG = 'KG',
  GM = 'GM',
  LTR = 'LTR',
  ML = 'ML',
  PCS = 'PCS',
  BOX = 'BOX',
  PACKET = 'PACKET',
  BOTTLE = 'BOTTLE',
  TRAY = 'TRAY',
}
